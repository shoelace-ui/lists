/**
 * Module dependencies
 */

@require 'shoelace-ui-line-height'

/**
 * Define `ul`, `ol`, and `dl` styles.
 */

ul
ol
  margin-top 0
  margin-bottom (line-height--computed / 2)
  ul
  ol
    margin-bottom 0
